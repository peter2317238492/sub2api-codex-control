#!/bin/sh
set -eu
/usr/libexec/sub2api-codex-connector/package-lifecycle verify-install "@VERSION@"
printf '%s\n' "Run sub2api-codex-connector-ctl init as each Connector user; no Codex files were changed."
