# Version matrix

## Current development tuple

This table records the current repository contracts and their production gates;
it is not proof that the production host already satisfies them.
`versions.lock.json` is the machine-readable source for the Codex/Sub2API tuple.

| Component | Development value | Production admission rule |
| --- | --- | --- |
| Control API | `0.1.12` (pins the Connector release's own signed serialization of `connector-release-metadata.json` — sorted, two-space indented, newline terminated, exactly what `connector/release/release.py` writes and what `server_package.py verify-release` already admitted — in both production admission paths (`lifecycle.py` and `deployment-admission.py`), and projects it into the deployment environment as one compact canonical line; `control-v0.1.11` is complete and verified on the production host: id 383156945, run 33948598180 attempt 1, source commit `a0238f4643bd31cdffadd080733ad633aca0bceb`, but its lifecycle install rejected every genuine server package before touching production (`Connector release metadata is not canonical JSON`) because both admission paths demanded the Control release's compact record form for bytes the Connector release signs indented, and the unit fixtures had been written in that same compact form; `0.1.11` admits the official Sub2API image exactly as published: pins the `/app/docker-entrypoint.sh` wrapper that `exec`s `/app/sub2api` by hash in `versions.lock.json` and verifies it inside the running container, admits exactly the lock-pinned bounded `noexec,nosuid,nodev` tmpfs at `/tmp` that Sub2API's gateway needs for its response spool on a read-only root filesystem — the first immutable production rollout on `control-v0.1.10` started the candidate, whose OpenAI relay then failed every request with `read-only file system` until an operator tmpfs mitigation, and its runtime verification rejected the wrapper — and binds Compose 5's `sha256:`-prefixed `com.docker.compose.image` label to the locked image where Compose 2 wrote bare hex; `control-v0.1.10` is complete and verified on the production host: id 383142451, run 33945526019 attempt 1, source commit `9ef3dfb4d2071aa00c4ea18a78de470596c9ecb1`; `0.1.10` runs the rollback-snapshot `docker container commit` bare against the already-stopped container — Docker CLI 29 prints a deprecation notice for the pause flags on stdout, which corrupted the captured image ID and aborted the first production immutable Sub2API migration inside its bounded automatic rollback — and keeps the health poller from overwriting the abort handlers' captured exit status; `control-v0.1.9` is complete and verified on the production host: id 383128701, run 33942365841 attempt 1, source commit `03df210670297d0c1ef31566c7319a12d689751a`; `0.1.9` re-freezes the Sub2API tuple to `0.2.0`; also lets the operator environment carry the explicit `SUB2API_EXPECTED_DATA_BIND_*` policy for a bind-mounted Sub2API data directory, admits a Sub2API config owned by that bind user, and root-copies the Compose service secrets for the root-only recovery checks — the three gaps a bootstrap-deployed host exposed before any formal install could pass; `control-v0.1.8` is the first complete immutable Control release: id 380246492, 84 assets, run 33474040010 attempt 1, source commit `c0e954a83cf7708e22d309cb2eda4395b5c01d55`; `control-v0.1.0` through `control-v0.1.7` burned: their pipeline runs failed — v0.1.0 because the release validate job never installed the pinned Codex CLI that `scripts/verify-contracts.mjs` requires, v0.1.1 because the source bundle's top-level allowlist predated `.gitattributes` and rejected the release tree, v0.1.2 because Cosign v3 removed the `-a` annotation flag from `attest`/`verify-attestation` that the sign and verify paths still passed, v0.1.3 because Cosign v3 renders local-image signature claims without the digest or annotations so the offline digest-binding assertions read empty values (the binding now reads the saved layout's index and v3 sign statement directly), v0.1.4 because the package-build job saved the public vulnerability receipt under a temporary name the release tool's exact-basename validation rejects; the systematic audit that followed fixed eight further never-executed-path defects before they could each burn a tag (descriptor output naming, missing package output directory, compact-versus-indented connector canonical JSON, the connector's `.intoto.json` provenance naming, world-writable `/tmp` extraction ancestors, zero-length pinned Trivy no-op files, a hard-coded evidence-inventory count that assumed a two-part offline package, and buildx wrapping promoted manifests in fresh single-entry indexes) — and formal tags are immutable, v0.1.5 because the package tool's private-key scan flagged the PEM marker literals in its own packaged source; the container preflight harness that followed reproduced the whole package-build, sign-validation, scan, and receipt pipeline locally and fixed seven further defects before they could each burn a tag (the lock's SBOM records carry no size field, crane's OCI 1.1 artifactType descriptor key, the Go root module's legitimately empty version, upstream-versus-composed OS package versions in vulnerability binding, one package legitimately installed at several paths, mixed deb and apk inventories breaking Trivy's SBOM decoder, and per-path duplicate findings), replaced the disposition binding that demanded run-time hashes and the containing commit itself (self-referentially impossible to pre-author) with identity-plus-review-term binding under the receipt's own hashes, moved every severity into the disposition-reviewed class because the pinned Debian and Alpine bases carry upstream-unfixed criticals no hard block could ever admit, pinned both Trivy databases by manifest digest so the committed 548-entry disposition review is exactly the set the release scans, and staged the Trivy cache from the pinned pulled layer, v0.1.6 because the workflow-hardcoded database pins lagged the upstream tags that advance every few hours (the digest guard failed exactly as designed; the pins now live in repository variables refreshed against the reviewed dispositions right before tagging), v0.1.7 reached the publish job — the first run with signing, all six scans, the 553-entry disposition review, and the signed receipt all green — and failed only on the derived inventory count reading the server package manifest from the vulnerability evidence directory, whose transferred artifact deliberately carries only the unique files (the count now reads the signed server package artifact present in the same job) — and formal tags are immutable) | API image digest and source commit recorded |
| PWA | `0.1.12` (versioned together with the Control API release tag) | PWA image digest built from the same release commit |
| Connector | `0.1.11` (`connector-v0.1.0` through `connector-v0.1.10` burned: their pipeline runs failed — v0.1.0 on a missing output parent, v0.1.1 on a mawk-incompatible awk expression, v0.1.2 on rpmbuild's brp-strip mutating the packaged binary, v0.1.3 on removing the read-only release module cache, v0.1.4 on artifact transfers stripping the executable bit into a pipeline whose macOS finalize stage had been removed without a Linux replacement, v0.1.5 on the sign stage unconditionally validating Apple identity inputs the Linux matrix never provides, v0.1.6 on a self-defeating asset-name validation in the scanner installer, v0.1.7 on GitHub rejecting the zero-length pinned-empty scanner evidence uploads, v0.1.8 published a fully valid immutable release whose verifier wrongly expected the commit rather than the annotated tag object in GitHub's attestation subject, v0.1.9 on the descriptor canonicalizer demanding exact keys on GitHub's decorated asset objects, v0.1.10 on the vulnerability manifest deriving a null artifact filename that the aggregate binds by name — and formal tags are immutable); executable protected-tag pipeline and local fail-closed tests in `connector/release/` | selected artifact passes the verifier against externally pinned GitHub issuer/identity/source SHA/trigger and the pinned RPM OpenPGP fingerprint; immutable release and all required bundles/evidence exist; trusted release run recorded: `connector-v0.1.11` (immutable release 379647180, workflow run 33377004159 attempt 1, source commit `e579421f3c1d987acb56b64a094bf6a3984cb170`) |
| Control envelope | version `1` | unknown versions rejected before dispatch |
| Codex CLI | exactly `0.147.0` | Connector version check succeeds before app-server start |
| app-server v2 schema | SHA-256 `511c1b3ca038a80740a5a41ca10a7f925c0f744e582fb9aaa03cc46c6e98b80b` | generated bundle reproduces and Connector config carries this digest |
| Sub2API official image | linux/amd64 `weishaw/sub2api@sha256:271bb3b34661803681cabf54e99811ab8e248b0dd4c88b09ea1226e22dea5751` (reported as the image ID under the containerd image store), multi-platform index `sha256:553864545ec1b446c4b3e3b394599523463de2ff93170b4a5b0ad00026c8b945`, Docker Created `2026-09-02T03:23:33.546935291Z`, labels `0.2.0/aa236488` (re-frozen from `0.1.178/e0c48a19` on 2026-09-05 after production's Sub2API self-updated in place; the live PID 1 binary was byte-identical to the official `0.2.0` image binary) | `immutable-image-v1` only; exact platform manifest, image ID, labels, digest-only `Config.Image`, and stable live container identity |
| Sub2API runtime binary | `0.2.0`, commit `aa236488351eb71e120fc2b6fb32e36b0374c918`, build `2026-09-02T03:13:57Z`, size `119423136`, SHA-256 `32536062df62388fd508c73d5fa94f84ff6bdd38837dd175ba83e26b4e711bc3` | official amd64 image and release archive are byte-identical; production admission must separately prove the live PID 1 executable matches |
| Sub2API writable layer and data | no image-layer drift; exactly one writable data mount at `/app/data` | named volume, or an explicitly admitted canonical bind whose source/owner/group/mode and `rprivate` propagation match exactly; every ancestor before the final source is root-owned; symlinks, group/other-writable path components, updater artifacts, unexpected diff, or any additional mount fail |
| Sub2API browser token keys | access `auth_token`, refresh `refresh_token` | PWA sends only access token to exchange; refresh never enters Control API |
| Sub2API auth contract | `docs/contracts/sub2api-auth.v0.2.0.json`, SHA-256 `e69ecf9536f21271121c3b000d1b40562fd10618ad79a5ebe848e472e24d4c21` (the five audited upstream auth sources are byte-identical between `e0c48a19` and `aa236488`, so the contract carries over verbatim); `GET /api/v1/auth/me`, `POST /api/v1/auth/refresh`, and `POST /api/v1/auth/logout`; disabled, TokenVersion-revoked, and session-binding failures use frozen `USER_INACTIVE`, `TOKEN_REVOKED`, and `SESSION_BINDING_MISMATCH` codes | lock and contract hashes match; exchange forwards the browser IP/User-Agent through the same-host Nginx overwrite policy |
| PostgreSQL tools | `0.1.0`, built from `postgres:18-alpine@sha256:9a8afca54e7861fd90fab5fdf4c42477a6b1cb7d293595148e674e0a3181de15` for compatibility with the production PostgreSQL 18.4 dump format | PostgreSQL-tools image digest is in the same signed atomic release lock as API/PWA; exact repository policy, signature, SPDX SBOM, SLSA provenance, OCI labels, `pg_dump`/`pg_restore` major version, and `linux/amd64` platform verify before backup/restore rehearsal |
| Redis test dependency | `redis:7-alpine@sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99` | production Redis exact version separately recorded; ACL isolation passes |
| Nginx | `nginx:1.28-alpine@sha256:a8b39bd9cf0f83869a2162827a0caf6137ddf759d50a171451b335cecc87d236` | `nginx -t` and security smoke pass |
| Python | `python:3.12-slim-bookworm@sha256:8a7e7cc04fd3e2bd787f7f24e22d5d119aa590d429b50c95dfe12b3abe52f48b`; constraints in `deploy/constraints/control-api.txt` | constraints update is explicit and locked dependency/SBOM scan passes |
| Node/pnpm | `node:22-alpine@sha256:16e22a550f3863206a3f701448c45f7912c6896a62de43add43bb9c86130c3e2`, pnpm `11.7.0` | frozen lockfile build reproduces PWA assets |
| Go | exactly `1.26.5` for Connector releases (`go.mod` retains the `1.24.0` language baseline); portable levels `GOAMD64=v1`, `GOARM64=v8.0`; `GOENV=off`, `GOFIPS140=off`, `GOTOOLCHAIN=local` | `go.sum`, two-pass reproducible candidates, tests, per-artifact SPDX/SLSA evidence, and signed manifest verification pass |

## Dated production drift evidence

The 2026-08-12 read-only audit observed base image
`sha256:2ca591c2...d6c8` with labels `0.1.151/deff3123`, a self-updated
`0.1.175/93c32fa1` executable, and writable updater artifacts. This is retained
as non-admitting historical evidence. It must fail the formal gate and does not
prove the current production host runs the `0.2.0` immutable tuple.

## Feature admission

| Capability | Current implementation evidence | Production release evidence required |
| --- | --- | --- |
| Same-origin PWA and HTTP session exchange | implemented, including CSRF/session revocation and renewal through the observed Sub2API refresh contract; refresh credentials never enter the Control API | authenticated identity/refresh/logout smoke against the admitted Sub2API image and machine-readable refresh-contract lock |
| PostgreSQL/Redis readiness | implemented with PostgreSQL as the durable authority and Redis as namespaced coordination | dedicated production role/ACL verification and failure injection |
| `/codex-ws/browser` | implemented with authenticated, user-scoped catch-up and Redis Pub/Sub fanout; automated edge and scope coverage exists | production same-origin smoke plus reconnect cursor/load result |
| `/codex-ws/device` | implemented with signed auth, persistent outbox, replay/ACK, gap close, heartbeat, reconnect, and revocation; the disposable TLS harness drives a real Connector | production TLS/heartbeat/reconnect/revocation result against the release artifacts |
| Multiple API workers/replicas | cross-instance browser/device fanout, atomic connection ownership, replacement, and durable dispatch are implemented; production starts two API services with one worker each | deterministic two-replica test with device WSS on one replica and HTTP/browser WSS on the other, followed by load and kill/reconnect results, before increasing `CONTROL_API_WORKERS` |
| Remote turn control | implemented for exactly the eight typed commands with ownership, field projection, idempotency, restart recovery, and dangerous-method denial; the disposable harness audits the dispatched method set | authenticated production canary against the pinned Codex release |
| Remote approvals | implemented with bounded projection, one-shot decisions, epoch/ownership binding, disconnect handling, and a maximum 120-second fail-closed timeout | production command/file/permission approval and timeout result |
| Observability | bounded Control API HTTP, authentication, pairing, WebSocket, command, reconciliation, maintenance, dependency, and pool metrics plus the private atomic Connector textfile metrics are implemented | wire and verify production Prometheus targets, external synthetic/datastore/migration/backup/restore/policy collectors, Alertmanager delivery, and controlled firing/recovery evidence |
| Connector automatic updates | prohibited in MVP | signed manifest, atomic rollback, staged rollout, and key-revocation drill |

## Release review

For each release, copy the development tuple into a dated, immutable release
record and attach:

- source commit and clean-tree evidence;
- API/PWA/PostgreSQL-tools image digests, signatures, SBOMs, provenance, and
  vulnerability disposition;
- Connector artifact digests, signatures, provenance, and native package signatures;
- Sub2API image digest, binary-reported version, commit, and captured `/auth/me`
  success/disabled/revoked fixtures;
- app-server schema regeneration result and aggregate digest;
- Alembic current/head revisions plus pre-migration backup checksum;
- authenticated smoke, dangerous-path denial, WebSocket, reconnect, approval,
  and ordinary Codex App/CLI coexistence results.

Any mismatch is a hard failure. Do not silently widen ranges, fall back to raw
RPC, or start app-server when Codex/schema verification fails.
